//Program to calculate largest of two numbers using if condition  
#include <stdio.h> 
int main() 
{ 
            int a=10, b=20, max; 
            max = a;	 
            if(b > max) 
                  max=b; 
            printf("Larger number is  %d\n",max); 
}  